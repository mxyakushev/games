import React from 'react';
import { Box } from '@chakra-ui/react';
import { MemoryBoard } from '../board';

export const Memory = () => {
  return (
    <Box>
      <MemoryBoard />
    </Box>
  );
};
