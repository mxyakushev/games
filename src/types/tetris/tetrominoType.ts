import { KeyChar } from 'types';

export interface ITetromino {
  0: {
    shape: KeyChar[][];
    color: string;
  };
  I: {
    shape: KeyChar[][];
    color: string;
  };
  J: {
    shape: KeyChar[][];
    color: string;
  };
  L: {
    shape: KeyChar[][];
    color: string;
  };
  O: {
    shape: KeyChar[][];
    color: string;
  };
  S: {
    shape: KeyChar[][];
    color: string;
  };
  T: {
    shape: KeyChar[][];
    color: string;
  };
  Z: {
    shape: KeyChar[][];
    color: string;
  };
}
